export const metadata = {
    title: "Harvest Table · San Francisco",
    description: "Seasonal ingredients. Honest cooking. Good company.",
};

export default function SampleLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
